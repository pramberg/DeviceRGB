# Ignore everything since I'm unsure if I'm allowed to distribute the SDKs. Anyways, just unzip the desired SDKs in this directory, and the build system should handle the rest (in the future at least).
*
!.gitignore